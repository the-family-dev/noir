{
  "watch": ["server/**/*"],
  "ext": "ts,json",
  "ignore": ["node_modules", ".next"],
  "exec": "ts-node -r tsconfig-paths/register --project tsconfig.server.json server/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
